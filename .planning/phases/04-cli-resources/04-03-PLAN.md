---
phase: 04-cli-resources
plan: 03
type: execute
wave: 3
depends_on: [04-01, 04-02]
files_modified:
  - .planning/REQUIREMENTS.md
autonomous: true
gap_closure: true
requirements: [CLI-01, CLI-02]

must_haves:
  truths:
    - "Running `memblocks-cli --help` from the terminal prints help text without a ModuleNotFoundError traceback"
    - "Running `memblocks-cli set-block <id>` writes to the shared state file without error"
    - "Running `memblocks-cli get-block` prints the current active block without error"
    - "REQUIREMENTS.md CLI-01 and CLI-02 descriptions accurately reflect the `memblocks-cli` command name — no stale `memblocks` references"
  artifacts:
    - path: ".venv/Lib/site-packages/_memblocks_mcp.pth"
      provides: "Editable install path entry so mcp_server is importable when running memblocks-cli.exe directly"
      contains: "C:\\Users\\Lenovo\\Desktop\\MemBlocks"
    - path: ".planning/REQUIREMENTS.md"
      provides: "Accurate CLI requirement descriptions matching the implemented command name"
      contains: "memblocks-cli"
  key_links:
    - from: ".venv/Scripts/memblocks-cli.exe"
      to: "mcp_server.cli:main"
      via: "Python sys.path populated by _memblocks_mcp.pth"
      pattern: "memblocks-cli --help"
---

<objective>
Close the single failing truth from Phase 4 verification: the `memblocks-cli` binary crashes with `ModuleNotFoundError` due to an empty editable install `.pth` file, and REQUIREMENTS.md CLI-01/CLI-02 descriptions still reference the old `memblocks` command name instead of the implemented `memblocks-cli` name.

Purpose: Make the CLI binary actually runnable from the terminal and align documentation with the deliberate architectural decision to use `memblocks-cli` (logged in STATE.md and both SUMMARYs).
Output: A working `memblocks-cli` binary + accurate requirements documentation.
</objective>

<execution_context>
@C:/Users/Lenovo/.config/opencode/get-shit-done/workflows/execute-plan.md
@C:/Users/Lenovo/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md

<!-- Gap being closed -->
@.planning/phases/04-cli-resources/04-VERIFICATION.md

<!-- What was built -->
@.planning/phases/04-cli-resources/04-01-SUMMARY.md

<!-- Key source files -->
@mcp_server/pyproject.toml
@mcp_server/cli.py
</context>

<tasks>

<task type="auto">
  <name>Task 1: Fix broken editable install so memblocks-cli binary works</name>
  <files>.venv/Lib/site-packages/_memblocks_mcp.pth</files>
  <action>
    The `.pth` file at `.venv/Lib/site-packages/_memblocks_mcp.pth` is 0 bytes. When `memblocks-cli.exe` runs directly (not via `uv run`), Python cannot find `mcp_server` because the editable install path was never written. This causes `ModuleNotFoundError: No module named 'mcp_server'`.

    Fix: Reinstall the editable package from the workspace root to regenerate the `.pth` file:

    ```
    uv pip install -e mcp_server
    ```

    Run this from the workspace root (`C:\Users\Lenovo\Desktop\MemBlocks`). The `uv pip install -e` command will rewrite `_memblocks_mcp.pth` with the correct path (workspace root: `C:\Users\Lenovo\Desktop\MemBlocks`), making `mcp_server` importable when running the `.exe` directly.

    After running the install, verify:
    1. `_memblocks_mcp.pth` is no longer empty — it should contain the workspace root path
    2. `memblocks-cli --help` prints the argparse help without any traceback

    Do NOT manually edit the `.pth` file — let `uv pip install -e` regenerate it correctly. If `uv pip install -e mcp_server` fails, try `uv pip install -e ./mcp_server` (explicit relative path).
  </action>
  <verify>
    <automated>
      uv pip install -e mcp_server && memblocks-cli --help
    </automated>
  </verify>
  <done>
    - `.venv/Lib/site-packages/_memblocks_mcp.pth` is non-empty (contains workspace root path)
    - `memblocks-cli --help` prints usage text showing `set-block` and `get-block` subcommands
    - `memblocks-cli set-block --help` exits 0 without any traceback
  </done>
</task>

<task type="auto">
  <name>Task 2: Update REQUIREMENTS.md to reflect memblocks-cli command name</name>
  <files>.planning/REQUIREMENTS.md</files>
  <action>
    REQUIREMENTS.md CLI-01 and CLI-02 currently say `memblocks set-block` and `memblocks get-block`. The implemented command (a deliberate architectural decision logged in STATE.md, both SUMMARYs, and pyproject.toml) is `memblocks-cli`. Update two descriptions to eliminate the stale `memblocks` references:

    **CLI-01** (line 38): Change from:
    > User can set the active block by block ID via `memblocks set-block <block_id>`, which writes to the shared state file

    To:
    > User can set the active block by block ID via `memblocks-cli set-block <block_id>`, which writes to the shared state file

    **CLI-02** (line 39): Change from:
    > User can view the current active block (name, ID) via `memblocks get-block`

    To:
    > User can view the current active block (name, ID) via `memblocks-cli get-block`

    Also update the "Last updated" date at the bottom of the file to today: `2026-03-14`.

    No other changes — STOR-01 and STOR-02 remain unchecked as-is (those are separate store tools requirements unrelated to this gap).
  </action>
  <verify>
    <automated>
      python -c "content = open('.planning/REQUIREMENTS.md').read(); assert 'memblocks-cli set-block' in content, 'CLI-01 not updated'; assert 'memblocks-cli get-block' in content, 'CLI-02 not updated'; assert 'memblocks set-block' not in content, 'stale reference remains'; print('REQUIREMENTS.md updated correctly')"
    </automated>
  </verify>
  <done>
    - CLI-01 description contains `memblocks-cli set-block <block_id>`
    - CLI-02 description contains `memblocks-cli get-block`
    - No stale `memblocks set-block` or `memblocks get-block` references remain in the CLI section
    - All other requirement entries are unchanged
  </done>
</task>

</tasks>

<verification>
After both tasks complete:

1. `memblocks-cli --help` exits 0 and prints subcommand list (set-block, get-block)
2. `memblocks-cli set-block someblockid` writes to `~/.config/memblocks/active_block.json` without traceback
3. `memblocks-cli get-block` reads and prints the active block ID
4. `.planning/REQUIREMENTS.md` contains `memblocks-cli set-block` and `memblocks-cli get-block` in CLI-01/CLI-02
5. `_memblocks_mcp.pth` contains a non-empty path string

Truth #3 from VERIFICATION.md ("Both commands are accessible as `memblocks set-block` / `memblocks get-block`") is now closed: the binary works, and REQUIREMENTS.md reflects the actual command name. Score advances from 6/7 → 7/7.
</verification>

<success_criteria>
- `memblocks-cli --help` runs without error from any terminal (not just via `uv run`)
- `memblocks-cli set-block <id>` and `memblocks-cli get-block` both function end-to-end
- REQUIREMENTS.md CLI-01 and CLI-02 accurately describe the implemented `memblocks-cli` command
- Phase 4 verification gap is closed — all 7 truths satisfied
</success_criteria>

<output>
After completion, create `.planning/phases/04-cli-resources/04-03-SUMMARY.md` following the standard summary template.
</output>
