[project]
name = "memblocks-workspace"
version = "0.1.0"
description = "UV workspace root for memBlocks monorepo"
readme = "README.md"
requires-python = ">=3.11"

[tool.uv.workspace]
members = ["memblocks_lib", "backend"]

[tool.uv.sources]
memblocks = { workspace = true }
